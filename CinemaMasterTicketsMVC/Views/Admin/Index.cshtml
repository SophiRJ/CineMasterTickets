@* @model IEnumerable<CinemaMasterTicketsMVC.ViewModels.AdminUserViewModel>

@{
    ViewData["Title"] = "Panel de Administrador";

    var empleados = Model.Where(u => u.IsEmployee).ToList();
    var clientes = Model.Where(u => u.IsCustomer).ToList();
}

<h2 class="mb-4">Gestión de Usuarios</h2>

<!-- ===================== -->
<!-- BOTONES SUPERIORES -->
<!-- ===================== -->
<div class="d-flex justify-content-end gap-2 mb-4">
    <a asp-action="CreateEmployee" class="btn btn-success">
        Crear Nuevo Empleado
    </a>

    <a asp-action="Home" asp-controller="Admin" class="btn btn-secondary">
        Volver al Panel
    </a>
</div>

<!-- ===================== -->
<!-- EMPLEADOS -->
<!-- ===================== -->
<h3 class="mt-4 mb-3">Empleados</h3>

<table class="table table-striped align-middle">
    <thead class="table-dark">
        <tr>
            <th>Email</th>
            <th>Taquilla</th>
            <th style="width: 30%">Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in empleados)
        {
            <tr>
                <td>@user.Email</td>
                <td>
                    <span class="badge bg-info"></span>
                </td>
                <td>
                    <div class="d-flex gap-2">
                        <a asp-action="Edit"
                           asp-controller="Employee"
                           asp-route-id="@user.EmployeeId"
                           class="btn btn-warning btn-sm">
                            Editar
                        </a>

                        <a asp-action="DeleteEmployee"
                           asp-route-id="@user.EmployeeId"
                           class="btn btn-danger btn-sm"
                           onclick="return confirm('¿Seguro que deseas borrar este empleado?');">
                            Borrar
                        </a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

<!-- ===================== -->
<!-- CLIENTES -->
<!-- ===================== -->
<h3 class="mt-5 mb-3">Clientes</h3>

<table class="table table-striped align-middle">
    <thead class="table-dark">
        <tr>
            <th>Email</th>
            <th>Estado</th>
            <th style="width: 30%">Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in clientes)
        {
            <tr>
                <td>@user.Email</td>
                <td>
                    <span class="badge bg-success">Registrado</span>
                </td>
                <td>
                    <div class="d-flex gap-2">
                        <a asp-action="EditCustomer"
                           asp-route-id="@user.CustomerId"
                           class="btn btn-warning btn-sm">
                            Editar
                        </a>

                        <a asp-action="DeleteCustomer"
                           asp-route-id="@user.CustomerId"
                           class="btn btn-danger btn-sm"
                           onclick="return confirm('¿Seguro que deseas borrar este cliente?');">
                            Borrar
                        </a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

<!-- ===================== -->
<!-- ACCIONES -->
<!-- ===================== -->
@* <div class="mt-4 d-flex gap-2">
    <a asp-action="CreateEmployee" class="btn btn-success">
        Crear Nuevo Empleado
    </a>

   @*  <a asp-action="Roles" class="btn btn-secondary">
        Gestionar Roles
    </a> 
</div> *@
@model CinemaMasterTicketsMVC.ViewModels.AdminUsersPanelViewModel

@section Styles {
    <link rel="stylesheet" href="~/css/adminHome.css" />
}

@{
    ViewData["Title"] = "Gestión de Usuarios";
}

<div class="container management-container py-4">
    @Html.AntiForgeryToken()

    @* Cabecera Principal *@
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="text-white fw-bold"><i class="fas fa-users-cog me-2 text-warning"></i>Gestión de Usuarios</h2>
        <div class="d-flex gap-2">
            <a asp-action="CreateEmployee" class="btn btn-warning rounded-pill px-4">
                <i class="fas fa-user-plus me-2"></i>Nuevo Empleado
            </a>
            <a asp-action="Home" asp-controller="Admin" class="btn btn-outline-light rounded-pill px-4">
                <i class="fas fa-arrow-left me-2"></i>Panel Admin
            </a>
        </div>
    </div>

    @* Sección Empleados *@
    <div class="card bg-dark mb-5 border-secondary shadow-sm">
        <div class="card-header border-secondary">
            <h4 class="text-warning mb-0"><i class="fas fa-user-tie me-2"></i>Staff del Cine</h4>
        </div>
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table align-middle">
                    <thead>
                        <tr>
                            <th class="ps-4">Nombre y Apellidos</th>
                            <th>Email</th>
                            <th>Taquilla</th>
                            <th class="text-end pe-4">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var emp in Model.Employees)
                        {
                            <tr id="row-employee-@emp.EmployeeId">
                                <td class="ps-4 fw-bold text-white">@emp.Firstname @emp.Lastname</td>
                                <td class="text-white-50">@emp.Email</td>
                                <td>
                                    <span class="badge rounded-pill bg-info">
                                        <i class="fas fa-desktop me-1"></i>@(emp.BoxOffice?.BoxOfficeName ?? "No asignada")
                                    </span>
                                </td>
                                <td class="text-end pe-4">
                                    <div class="btn-group gap-1">
                                        <a asp-action="Index" asp-controller="Employee" asp-route-id="@emp.EmployeeId" class="btn btn-sm btn-outline-info" title="Ver Perfil"><i class="fas fa-eye"></i></a>
                                        <a asp-action="Edit" asp-controller="Employee" asp-route-id="@emp.EmployeeId" class="btn btn-sm btn-outline-warning"><i class="fas fa-edit"></i></a>
                                        <button type="button" class="btn-delete-item btn btn-sm btn-outline-danger" data-id="@emp.EmployeeId" data-name="@emp.Firstname" data-type="Employee">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    @* Sección Clientes *@
    <div class="card bg-dark border-secondary shadow-sm">
        <div class="card-header border-secondary">
            <h4 class="text-danger mb-0"><i class="fas fa-user-friends me-2"></i>Clientes del Club</h4>
        </div>
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table align-middle">
                    <thead>
                        <tr>
                            <th class="ps-4">Cliente</th>
                            <th>Email</th>
                            <th>Puntos</th>
                            <th class="text-end pe-4">Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var cli in Model.Customers)
                        {
                            <tr id="row-customer-@cli.CustomerId">
                                <td class="ps-4 text-white">@cli.FirstName @cli.LastName</td>
                                <td class="text-white-50">@cli.Email</td>
                                <td><span class="text-warning-points"><i class="fas fa-ticket-alt me-2"></i>@cli.FidelityPoints</span></td>
                                <td class="text-end pe-4">
                                    <div class="btn-group gap-1">
                                        <a asp-action="Index" asp-controller="Customer" asp-route-id="@cli.CustomerId" class="btn btn-sm btn-outline-info"><i class="fas fa-eye"></i></a>
                                        <button type="button" class="btn-delete-item btn btn-sm btn-outline-danger" data-id="@cli.CustomerId" data-name="@cli.FirstName" data-type="Customer">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/admin-managament.js"></script>
}